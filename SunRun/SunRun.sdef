#include "${CURDIR}/../../../mangOH.sdef"

/*
 * You can build this SDEF by running the following command from your top-level
 * mangOH directory:
 * MANGOH_ROOT=`pwd` MANGOH_BOARD=yellow mksys -t wp76xx samples/Demos/SunRun/SunRun.sdef
 */

apps:
{
#if ${MANGOH_BOARD} = yellow
    // Octave apps
    ${CURDIR}/cloudInterface.${LEGATO_TARGET}.app
    ${CURDIR}/actions.${LEGATO_TARGET}.app

    ${CURDIR}/apps/locationService/combainLocation
    ${CURDIR}/apps/locationService/location
#endif
}
